@{
    ViewData["Title"] = "Buoni Regalo";
    var popularSalons = ViewBag.PopularSalons as IEnumerable<HaveASeat.Models.Salone> ?? new List<HaveASeat.Models.Salone>();
}

@section Styles {
    <style>
        .gift-card-preview {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 16px;
            position: relative;
            overflow: hidden;
        }

            .gift-card-preview::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.1'%3E%3Ccircle cx='30' cy='30' r='4'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E") repeat;
                opacity: 0.3;
            }

        .amount-option {
            transition: all 0.3s ease;
            cursor: pointer;
        }

            .amount-option:hover {
                transform: translateY(-2px);
                box-shadow: 0 8px 25px rgba(0,0,0,0.15);
            }

            .amount-option.selected {
                border-color: #7c3aed;
                background: linear-gradient(135deg, #7c3aed, #ec4899);
                color: white;
                transform: translateY(-2px);
                box-shadow: 0 8px 25px rgba(124,58,237,0.3);
            }

        .floating-element {
            animation: float 6s ease-in-out infinite;
        }

            .floating-element:nth-child(2) {
                animation-delay: -2s;
            }

            .floating-element:nth-child(3) {
                animation-delay: -4s;
            }

        @@keyframes float {
            0%, 100%

        {
            transform: translateY(0px) rotate(0deg);
        }

        33% {
            transform: translateY(-10px) rotate(2deg);
        }

        66% {
            transform: translateY(5px) rotate(-1deg);
        }

        }

        .salon-card-mini {
            transition: all 0.3s ease;
        }

            .salon-card-mini:hover {
                transform: translateY(-4px);
                box-shadow: 0 12px 30px rgba(0,0,0,0.15);
            }

        .hero-gradient {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
        }
    </style>
}

<!-- Hero Section -->
<section class="hero-gradient text-white py-16 md:py-24 relative overflow-hidden">
    <!-- Elementi fluttuanti decorativi -->
    <div class="absolute inset-0 pointer-events-none">
        <div class="floating-element absolute top-20 left-10 w-16 h-16 bg-white/10 rounded-full"></div>
        <div class="floating-element absolute top-40 right-20 w-12 h-12 bg-white/15 rounded-lg rotate-45"></div>
        <div class="floating-element absolute bottom-32 left-1/4 w-8 h-8 bg-white/20 rounded-full"></div>
        <div class="floating-element absolute bottom-20 right-1/4 w-20 h-20 bg-white/5 rounded-full"></div>
    </div>

    <div class="container mx-auto px-4 relative z-10">
        <div class="max-w-6xl mx-auto">
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
                <!-- Contenuto testuale -->
                <div class="text-center lg:text-left">
                    <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 leading-tight">
                        Il regalo perfetto per
                        <span class="bg-gradient-to-r from-yellow-300 to-orange-300 bg-clip-text text-transparent">
                            ogni occasione
                        </span>
                    </h1>
                    <p class="text-xl md:text-2xl mb-8 text-white/90 leading-relaxed">
                        Regala momenti di bellezza e benessere con i buoni regalo HaveASeat.
                        Validi in tutti i saloni partner.
                    </p>

                    <div class="flex flex-col sm:flex-row gap-4 mb-8">
                        <a href="#create-gift-card"
                           class="bg-white text-purple-600 px-8 py-4 rounded-full font-semibold
                                  hover:bg-gray-100 transition-all duration-300 transform hover:scale-105
                                  shadow-lg">
                            <i class="fas fa-gift mr-2"></i>
                            Crea Buono Regalo
                        </a>
                        <a href="#redeem"
                           class="border-2 border-white text-white px-8 py-4 rounded-full font-semibold
                                  hover:bg-white hover:text-purple-600 transition-all duration-300">
                            <i class="fas fa-ticket-alt mr-2"></i>
                            Riscatta Buono
                        </a>
                    </div>

                    <!-- Statistiche -->
                    <div class="grid grid-cols-3 gap-4 mt-12">
                        <div class="text-center">
                            <div class="text-2xl font-bold mb-1">10K+</div>
                            <div class="text-sm text-white/80">Saloni Partner</div>
                        </div>
                        <div class="text-center">
                            <div class="text-2xl font-bold mb-1">50K+</div>
                            <div class="text-sm text-white/80">Buoni Venduti</div>
                        </div>
                        <div class="text-center">
                            <div class="text-2xl font-bold mb-1">98%</div>
                            <div class="text-sm text-white/80">Soddisfazione</div>
                        </div>
                    </div>
                </div>

                <!-- Anteprima Gift Card -->
                <div class="flex justify-center lg:justify-end">
                    <div class="gift-card-preview w-80 h-48 p-6 text-white relative z-10">
                        <div class="flex justify-between items-start mb-4 relative z-10">
                            <div class="text-sm font-medium opacity-90">BUONO REGALO</div>
                            <div class="text-right">
                                <div class="text-2xl font-bold">€50</div>
                                <div class="text-xs opacity-75">HAVEASEAT</div>
                            </div>
                        </div>

                        <div class="absolute bottom-0 left-6 right-6 relative z-10">
                            <div class="text-xs opacity-75 mb-1">Valido fino al</div>
                            <div class="text-sm font-medium">@DateTime.Now.AddYears(1).ToString("dd/MM/yyyy")</div>
                            <div class="mt-3 text-xs font-mono tracking-wider">
                                XXXX-XXXX-XXXX
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Sezione Come Funziona -->
<section class="py-16 bg-gray-50 dark:bg-gray-800/50">
    <div class="container mx-auto px-4">
        <div class="max-w-4xl mx-auto text-center mb-12">
            <h2 class="text-3xl md:text-4xl font-bold mb-4 dark:text-white">Come funziona</h2>
            <p class="text-xl text-gray-600 dark:text-gray-300">Semplice, veloce e sicuro in soli 3 passaggi</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-5xl mx-auto">
            <!-- Step 1 -->
            <div class="text-center group">
                <div class="w-20 h-20 bg-purple-100 dark:bg-purple-900/50 rounded-full flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">
                    <div class="w-10 h-10 bg-purple-600 rounded-full flex items-center justify-center text-white font-bold text-lg">1</div>
                </div>
                <h3 class="text-xl font-semibold mb-3 dark:text-white">Scegli l'importo</h3>
                <p class="text-gray-600 dark:text-gray-300">Seleziona l'importo e il salone, oppure scegli un buono valido ovunque</p>
            </div>

            <!-- Step 2 -->
            <div class="text-center group">
                <div class="w-20 h-20 bg-purple-100 dark:bg-purple-900/50 rounded-full flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">
                    <div class="w-10 h-10 bg-purple-600 rounded-full flex items-center justify-center text-white font-bold text-lg">2</div>
                </div>
                <h3 class="text-xl font-semibold mb-3 dark:text-white">Personalizza</h3>
                <p class="text-gray-600 dark:text-gray-300">Aggiungi un messaggio personale e i dati del destinatario</p>
            </div>

            <!-- Step 3 -->
            <div class="text-center group">
                <div class="w-20 h-20 bg-purple-100 dark:bg-purple-900/50 rounded-full flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">
                    <div class="w-10 h-10 bg-purple-600 rounded-full flex items-center justify-center text-white font-bold text-lg">3</div>
                </div>
                <h3 class="text-xl font-semibold mb-3 dark:text-white">Invia o stampa</h3>
                <p class="text-gray-600 dark:text-gray-300">Il buono viene inviato via email o puoi stamparlo per un regalo fisico</p>
            </div>
        </div>
    </div>
</section>

<!-- Sezione Creazione Buono Regalo -->
<section id="create-gift-card" class="py-16 bg-white dark:bg-gray-800/80">
    <div class="container mx-auto px-4">
        <div class="max-w-6xl mx-auto">
            <div class="text-center mb-12">
                <h2 class="text-3xl md:text-4xl font-bold mb-4 dark:text-white">Crea il tuo buono regalo</h2>
                <p class="text-xl text-gray-600 dark:text-gray-300">Personalizza il regalo perfetto per chi ami</p>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
                <!-- Form di creazione -->
                <div class="space-y-6">
                    <!-- Scelta importo -->
                    <div>
                        <h3 class="text-lg font-semibold mb-4 dark:text-white">Scegli l'importo</h3>
                        <div class="grid grid-cols-2 md:grid-cols-4 gap-3 mb-4">
                            <div class="amount-option border-2 border-gray-200 dark:border-gray-600 rounded-lg p-4 text-center bg-white dark:bg-gray-700" data-amount="25">
                                <div class="text-lg font-bold dark:text-white">€25</div>
                            </div>
                            <div class="amount-option border-2 border-gray-200 dark:border-gray-600 rounded-lg p-4 text-center bg-white dark:bg-gray-700 selected" data-amount="50">
                                <div class="text-lg font-bold">€50</div>
                            </div>
                            <div class="amount-option border-2 border-gray-200 dark:border-gray-600 rounded-lg p-4 text-center bg-white dark:bg-gray-700" data-amount="75">
                                <div class="text-lg font-bold dark:text-white">€75</div>
                            </div>
                            <div class="amount-option border-2 border-gray-200 dark:border-gray-600 rounded-lg p-4 text-center bg-white dark:bg-gray-700" data-amount="100">
                                <div class="text-lg font-bold dark:text-white">€100</div>
                            </div>
                        </div>
                        <div class="flex items-center gap-2">
                            <span class="text-sm text-gray-600 dark:text-gray-300">Importo personalizzato:</span>
                            <input type="number" id="customAmount" min="10" max="1000"
                                   class="border border-gray-300 dark:border-gray-600 rounded-lg px-3 py-2 w-24 dark:bg-gray-700 dark:text-white"
                                   placeholder="€">
                        </div>
                    </div>

                    <!-- Tipo di buono -->
                    <div>
                        <h3 class="text-lg font-semibold mb-4 dark:text-white">Tipo di buono</h3>
                        <div class="space-y-3">
                            <label class="flex items-center p-4 border-2 border-gray-200 dark:border-gray-600 rounded-lg cursor-pointer hover:border-purple-300 dark:bg-gray-700">
                                <input type="radio" name="giftCardType" value="generic" checked class="mr-3">
                                <div>
                                    <div class="font-medium dark:text-white">Buono generico</div>
                                    <div class="text-sm text-gray-600 dark:text-gray-300">Valido in tutti i saloni partner</div>
                                </div>
                            </label>
                            <label class="flex items-center p-4 border-2 border-gray-200 dark:border-gray-600 rounded-lg cursor-pointer hover:border-purple-300 dark:bg-gray-700">
                                <input type="radio" name="giftCardType" value="specific" class="mr-3">
                                <div>
                                    <div class="font-medium dark:text-white">Salone specifico</div>
                                    <div class="text-sm text-gray-600 dark:text-gray-300">Valido solo nel salone selezionato</div>
                                </div>
                            </label>
                        </div>
                    </div>

                    <!-- Selezione salone (nascosto inizialmente) -->
                    <div id="salonSelection" class="hidden">
                        <h3 class="text-lg font-semibold mb-4 dark:text-white">Scegli il salone</h3>
                        <div class="relative">
                            <input type="text" id="salonSearch" placeholder="Cerca il tuo salone preferito..."
                                   class="w-full border border-gray-300 dark:border-gray-600 rounded-lg px-4 py-3 dark:bg-gray-700 dark:text-white">
                            <i class="fas fa-search absolute right-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
                        </div>
                    </div>

                    <a href="/GiftCard/Create"
                       class="w-full bg-purple-600 hover:bg-purple-700 text-white py-4 px-6 rounded-lg font-semibold
                              transition-all duration-300 transform hover:scale-105 shadow-lg inline-block text-center">
                        <i class="fas fa-arrow-right mr-2"></i>
                        Continua alla personalizzazione
                    </a>
                </div>

                <!-- Anteprima live -->
                <div class="flex justify-center items-start">
                    <div class="sticky top-8">
                        <h3 class="text-lg font-semibold mb-4 text-center dark:text-white">Anteprima del tuo buono</h3>
                        <div id="livePreview" class="gift-card-preview w-96 h-56 p-6 text-white relative">
                            <div class="flex justify-between items-start mb-4 relative z-10">
                                <div class="text-sm font-medium opacity-90">BUONO REGALO</div>
                                <div class="text-right">
                                    <div id="previewAmount" class="text-3xl font-bold">€50</div>
                                    <div class="text-xs opacity-75">HAVEASEAT</div>
                                </div>
                            </div>

                            <div class="relative z-10 mt-8">
                                <div id="previewSalon" class="text-sm font-medium mb-2">Valido in tutti i saloni partner</div>
                            </div>

                            <div class="absolute bottom-6 left-6 right-6 relative z-10">
                                <div class="text-xs opacity-75 mb-1">Valido fino al</div>
                                <div class="text-sm font-medium">@DateTime.Now.AddYears(1).ToString("dd/MM/yyyy")</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Saloni Popolari -->
@if (popularSalons.Any())
{
    <section class="py-16 bg-gray-50 dark:bg-gray-800/50">
        <div class="container mx-auto px-4">
            <div class="max-w-6xl mx-auto">
                <div class="text-center mb-12">
                    <h2 class="text-3xl md:text-4xl font-bold mb-4 dark:text-white">Saloni più richiesti</h2>
                    <p class="text-xl text-gray-600 dark:text-gray-300">Scopri i saloni più amati dai nostri clienti</p>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                    @foreach (var salon in popularSalons.Take(8))
                    {
                        <div class="salon-card-mini bg-white dark:bg-gray-800 rounded-xl overflow-hidden shadow-md">
                            <div class="relative h-32">
                                @if (salon.Immagini.Any(i => i.IsCover))
                                {
                                    <img src="@salon.Immagini.First(i => i.IsCover).Percorso"
                                         alt="@salon.Nome"
                                         class="w-full h-full object-cover">
                                }
                                else
                                {
                                    <div class="w-full h-full bg-gradient-to-br from-purple-400 to-pink-500 flex items-center justify-center">
                                        <i class="fas fa-store text-2xl text-white"></i>
                                    </div>
                                }
                            </div>

                            <div class="p-4">
                                <h3 class="font-semibold text-sm mb-1 dark:text-white truncate">@salon.Nome</h3>
                                <p class="text-xs text-gray-500 dark:text-gray-400 mb-2">@salon.Citta</p>
                                <div class="flex items-center justify-between">
                                    <div class="flex items-center">
                                        <div class="text-yellow-400 text-xs">
                                            @{
                                                var rating = salon.Recensioni.Any() ? salon.Recensioni.Average(r => r.Voto) : 0;
                                                for (int i = 1; i <= 5; i++)
                                                {
                                                    if (i <= rating)
                                                    {
                                                        <i class="fas fa-star"></i>
                                                    }
                                                    else
                                                    {
                                                        <i class="far fa-star"></i>
                                                    }
                                                }
                                            }
                                        </div>
                                        <span class="text-xs text-gray-500 dark:text-gray-400 ml-1">
                                            (@salon.Recensioni.Count)
                                        </span>
                                    </div>
                                    <a href="/GiftCard/Create?saloneId=@salon.SaloneId"
                                       class="text-xs bg-purple-600 text-white px-2 py-1 rounded-full hover:bg-purple-700 transition-colors">
                                        Scegli
                                    </a>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </section>
}

<!-- Sezione Riscatta Buono -->
<section id="redeem" class="py-16 bg-white dark:bg-gray-800/80">
    <div class="container mx-auto px-4">
        <div class="max-w-4xl mx-auto text-center">
            <h2 class="text-3xl md:text-4xl font-bold mb-4 dark:text-white">Riscatta il tuo buono regalo</h2>
            <p class="text-xl text-gray-600 dark:text-gray-300 mb-8">
                Hai ricevuto un buono regalo? Inserisci il codice per iniziare a prenotare
            </p>

            <div class="max-w-md mx-auto">
                <form id="redeemForm" class="space-y-4">
                    <div class="relative">
                        <input type="text"
                               id="redeemCode"
                               placeholder="Inserisci il codice (es. XXXX-XXXX-XXXX)"
                               maxlength="14"
                               class="w-full text-center text-lg font-mono tracking-wider border-2 border-gray-300 dark:border-gray-600
                                      rounded-lg px-4 py-4 uppercase dark:bg-gray-700 dark:text-white
                                      focus:border-purple-500 focus:ring-2 focus:ring-purple-200 transition-all">
                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                            <i class="fas fa-ticket-alt text-gray-400"></i>
                        </div>
                    </div>

                    <button type="submit"
                            class="w-full bg-purple-600 hover:bg-purple-700 text-white py-4 px-6 rounded-lg font-semibold
                                   transition-all duration-300 transform hover:scale-105 shadow-lg">
                        <i class="fas fa-check mr-2"></i>
                        Verifica Buono
                    </button>
                </form>

                <div class="mt-6 text-sm text-gray-500 dark:text-gray-400">
                    <p class="mb-2">💡 <strong>Suggerimento:</strong> Il codice si trova nell'email che hai ricevuto</p>
                    <p>📞 Hai problemi? <a href="#" class="text-purple-600 hover:underline">Contatta l'assistenza</a></p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- FAQ Section -->
<section class="py-16 bg-gray-50 dark:bg-gray-800/50">
    <div class="container mx-auto px-4">
        <div class="max-w-4xl mx-auto">
            <div class="text-center mb-12">
                <h2 class="text-3xl md:text-4xl font-bold mb-4 dark:text-white">Domande frequenti</h2>
                <p class="text-xl text-gray-600 dark:text-gray-300">Tutto quello che devi sapere sui buoni regalo</p>
            </div>

            <div class="space-y-4">
                <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md">
                    <button class="faq-question w-full text-left p-6 focus:outline-none" type="button">
                        <div class="flex justify-between items-center">
                            <span class="text-lg font-medium dark:text-white">Quanto tempo è valido un buono regalo?</span>
                            <i class="fas fa-chevron-down text-gray-400"></i>
                        </div>
                    </button>
                    <div class="faq-answer hidden px-6 pb-6 text-gray-600 dark:text-gray-300">
                        I buoni regalo HaveASeat sono validi per 12 mesi dalla data di acquisto.
                        La data di scadenza è chiaramente indicata sul buono.
                    </div>
                </div>

                <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md">
                    <button class="faq-question w-full text-left p-6 focus:outline-none" type="button">
                        <div class="flex justify-between items-center">
                            <span class="text-lg font-medium dark:text-white">Posso utilizzare il buono per qualsiasi servizio?</span>
                            <i class="fas fa-chevron-down text-gray-400"></i>
                        </div>
                    </button>
                    <div class="faq-answer hidden px-6 pb-6 text-gray-600 dark:text-gray-300">
                        Sì, puoi utilizzare il tuo buono regalo per qualsiasi servizio disponibile nei saloni partner,
                        purché il valore sia sufficiente a coprire il costo del trattamento.
                    </div>
                </div>

                <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md">
                    <button class="faq-question w-full text-left p-6 focus:outline-none" type="button">
                        <div class="flex justify-between items-center">
                            <span class="text-lg font-medium dark:text-white">Cosa succede se il servizio costa più del buono?</span>
                            <i class="fas fa-chevron-down text-gray-400"></i>
                        </div>
                    </button>
                    <div class="faq-answer hidden px-6 pb-6 text-gray-600 dark:text-gray-300">
                        Se il servizio scelto costa più del valore del buono, potrai pagare la differenza al momento della prenotazione
                        o direttamente presso il salone.
                    </div>
                </div>

                <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md">
                    <button class="faq-question w-full text-left p-6 focus:outline-none" type="button">
                        <div class="flex justify-between items-center">
                            <span class="text-lg font-medium dark:text-white">Posso richiedere il rimborso di un buono regalo?</span>
                            <i class="fas fa-chevron-down text-gray-400"></i>
                        </div>
                    </button>
                    <div class="faq-answer hidden px-6 pb-6 text-gray-600 dark:text-gray-300">
                        I buoni regalo non sono rimborsabili, ma possono essere trasferiti ad un'altra persona.
                        In caso di problemi, contatta il nostro servizio clienti.
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <script>
        $(document).ready(function() {
            let selectedAmount = 50;

            // Gestione selezione importo
            $('.amount-option').click(function() {
                $('.amount-option').removeClass('selected');
                $(this).addClass('selected');
                selectedAmount = $(this).data('amount');
                updatePreview();
                $('#customAmount').val('');
            });

            // Importo personalizzato
            $('#customAmount').on('input', function() {
                const customValue = parseFloat($(this).val());
                if (customValue >= 10 && customValue <= 1000) {
                    $('.amount-option').removeClass('selected');
                    selectedAmount = customValue;
                    updatePreview();
                }
            });

            // Gestione tipo buono
            $('input[name="giftCardType"]').change(function() {
                if ($(this).val() === 'specific') {
                    $('#salonSelection').removeClass('hidden');
                } else {
                    $('#salonSelection').addClass('hidden');
                    updatePreview();
                }
            });

            // Ricerca salone
            $('#salonSearch').on('input', function() {
                const query = $(this).val();
                if (query.length >= 2) {
                    // Implementa la ricerca dei saloni
                    // searchSalons(query);
                }
            });

            // Form riscatto buono
            $('#redeemForm').submit(function(e) {
                e.preventDefault();
                const code = $('#redeemCode').val().trim();

                if (!code) {
                    showAlert('Inserisci un codice valido', 'error');
                    return;
                }

                $.ajax({
                    url: '/GiftCard/Redeem',
                    type: 'POST',
                    data: { code: code },
                    success: function(response) {
                        if (response.success) {
                            showAlert('Buono valido! Reindirizzamento in corso...', 'success');
                            setTimeout(() => {
                                window.location.href = response.redirectUrl;
                            }, 2000);
                        } else {
                            showAlert(response.message, 'error');
                        }
                    },
                    error: function() {
                        showAlert('Errore durante la verifica. Riprova.', 'error');
                    }
                });
            });

            // Formattazione automatica codice
            $('#redeemCode').on('input', function() {
                let value = $(this).val().replace(/[^A-Z0-9]/g, '');
                if (value.length > 4) {
                    value = value.substring(0, 4) + '-' + value.substring(4);
                }
                if (value.length > 9) {
                    value = value.substring(0, 9) + '-' + value.substring(9);
                }
                $(this).val(value);
            });

            // FAQ toggle
            $('.faq-question').click(function() {
                const answer = $(this).siblings('.faq-answer');
                const icon = $(this).find('i');

                if (answer.hasClass('hidden')) {
                    answer.removeClass('hidden').slideDown();
                    icon.removeClass('fa-chevron-down').addClass('fa-chevron-up');
                } else {
                    answer.slideUp().addClass('hidden');
                    icon.removeClass('fa-chevron-up').addClass('fa-chevron-down');
                }
            });

            function updatePreview() {
                $('#previewAmount').text('€' + selectedAmount);

                const isGeneric = $('input[name="giftCardType"]:checked').val() === 'generic';
                if (isGeneric) {
                    $('#previewSalon').text('Valido in tutti i saloni partner');
                } else {
                    $('#previewSalon').text('Salone specifico selezionato');
                }
            }

            function showAlert(message, type) {
                const alertClass = type === 'success' ? 'bg-green-100 text-green-800 border-green-200' : 'bg-red-100 text-red-800 border-red-200';
                const icon = type === 'success' ? 'fa-check-circle' : 'fa-exclamation-triangle';

                const alert = $(`
                    <div class="fixed top-4 right-4 z-50 ${alertClass} border rounded-lg p-4 shadow-lg max-w-sm">
                        <div class="flex items-center">
                            <i class="fas ${icon} mr-2"></i>
                            <span>${message}</span>
                        </div>
                    </div>
                `);

                $('body').append(alert);

                setTimeout(() => {
                    alert.fadeOut(() => alert.remove());
                }, 5000);
            }

            // Smooth scroll per i link anchor
            $('a[href^="#"]').click(function(e) {
                e.preventDefault();
                const target = $($(this).attr('href'));
                if (target.length) {
                    $('html, body').animate({
                        scrollTop: target.offset().top - 100
                    }, 800);
                }
            });
        });
    </script>
}